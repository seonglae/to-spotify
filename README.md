# Basher

### TS CLI Tempate


### Build with
- commander


## Dev
```
npm i -g pnpm
pnpm i
pnpm run bin run blabla --bool --string blabla
```


## Install
```
pnpm build
npm install -g .
basher -v
basher run blabla --bool --string blabla
```